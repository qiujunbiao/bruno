info:
  name: 成员退出所有角色
  type: http
  seq: 7
  tags:
    - 角色

http:
  method: DELETE
  url: "{{baseUrl}}/v3/app/roles/users/:user_id"
  headers:
    - name: HAP-Appkey
      value: "{{apiKey}}"
  params:
    - name: user_id
      value: ""
      type: path
  auth:
    type: apikey
    key: HAP-Appkey
    value: "{{apiKey}}"
    placement: header

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: Successful response
    request:
      url: "{{baseUrl}}/v3/app/roles/users/:user_id"
      method: DELETE
      headers:
        - name: HAP-Appkey
          value: "{{apiKey}}"
      params:
        - name: user_id
          value: ""
          type: path
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: "{}"

docs: 将指定成员退出应用下所有角色
